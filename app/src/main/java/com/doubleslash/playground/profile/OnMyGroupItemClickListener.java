package com.doubleslash.playground.profile;

import android.view.View;

public interface OnMyGroupItemClickListener {
    public void onItemClick(MyGroupAdapter.ViewHolder holder, View view, int position);
}
